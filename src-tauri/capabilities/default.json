{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and prompt windows",
  "windows": ["main", "prompt"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "dialog:default"
  ]
}
