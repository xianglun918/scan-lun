# scan-lun 功能使用路径（Story Line）

> 按用户旅程介绍当前每个功能的使用路径：**怎么触发 → 界面做什么 → 结果去哪**。
> 领域术语定义见 [CONTEXT.md](./CONTEXT.md)，各功能需求/票见 [docs/specs/](./docs/specs/)。

## 1. 第一次使用

```
启动应用
  ├─ 系统托盘出现 scan-lun 图标（常驻）
  └─ 主窗口打开（标签：历史 | 设置）
```

- 默认设置：职场三省模板、触发时间 18:00、仅工作日开、开机自启关。
- 一切就绪，无需初始化；到点自动弹出，其余时间躲在托盘。

## 2. 每日定时提醒（核心链路）

```
到达触发时间（默认 18:00，周六日且开「仅工作日」时跳过）
  └─ 弹出 prompt 弹窗（无边框、置顶、抢焦点）
        ├─【保存】     → 三问写入本地库，弹窗关闭，当天不再打扰
        ├─【稍后提醒】 → 弹窗关闭，1 小时后重新弹出
        └─【跳过】     → 弹窗关闭，本次不再重弹、不落库
```

**已答当天不再打扰**：当日已有保存记录时，无论从哪条路径打开 prompt（到点提醒 / 托盘「立即填写」/ 稍后重发），都只显示「今日已完成，无需重复填写」，不会出现空白表单。

## 3. 系统托盘

| 操作 | 行为 |
|---|---|
| 左键单击图标 | 显示主窗口（历史/设置） |
| 菜单「打开 scan-lun」 | 显示主窗口 |
| 菜单「立即填写」 | 弹出 prompt（今日未答 → 空白表单；今日已答 → 「今日已完成」提示） |
| 菜单「退出」 | 真正退出应用 |

> 主窗口右上角「×」只是隐藏到托盘，不是退出；退出只能从托盘菜单。

## 4. 历史回看

```
主窗口 →「历史」标签
  ├─ 日期倒序列表（无记录时显示空态引导）
  ├─ 点日期行 → 展开当日三问答案（未答的问题显示「—」）
  └─ 顶部按钮
       ├─【导出 MD】→ 系统对话框选保存位置 → 按日期分节生成 .md
       └─【导出 CSV】→ 系统对话框选保存位置 → 生成表头 date,q1,q2,q3 的 .csv
```

导出为全量历史，格式跟随当前模板文案。

## 5. 设置

```
主窗口 →「设置」标签 → 修改后点【保存设置】
```

| 设置项 | 说明 | 生效方式 |
|---|---|---|
| 三省模板 | 三行文本，改问题文案 | 保存后弹窗占位符与历史展示同步变化 |
| 每日触发时间 | HH:MM | 保存后调度立即按新时间重算 |
| 仅工作日 | 勾选后周六日不提醒 | 保存后下次到点生效 |
| 开机自启 | 勾选后随系统启动（macOS LaunchAgent） | 保存时同步系统状态 |
| 清除全部数据 | 需二次确认，清空所有历史记录 | 不可撤销 |

## 6. 数据与隐私

- 存储：应用数据目录下 `scan-lun.db`（明文 SQLite）。macOS 为 `~/Library/Application Support/com.scanlun.app/scan-lun.db`，Windows/Linux 为对应系统数据目录。
- 数据完全在本机，无联网上传。
- 导出文件由你选择保存路径，内容为纯文本 Markdown / CSV。
